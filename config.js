const server_config = {
    port: 9090,
    host: 'localhost'
}

const db_config = {
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'simple_erp'
}

export {server_config ,db_config}